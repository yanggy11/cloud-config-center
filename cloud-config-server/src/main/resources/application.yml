server:
  port: 1980
spring:
  application:
    name: cloud-config
  cloud:
    config:
#      uri: http://localhost:1980
      server:
        git:
          uri: https://github.com/yanggy11/cloud-config-repo
